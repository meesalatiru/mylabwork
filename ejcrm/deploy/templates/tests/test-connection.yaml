apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ejcrm.fullname" . }}-test-connection"
  labels:
    {{- include "ejcrm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ejcrm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
